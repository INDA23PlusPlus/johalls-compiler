fn fib_contrived(n) {
    if n<2&&!(!1) {
        return (n & ~~3);
    } else {
        return fib_contrived(n - 1) + fib_contrived(n - 2);
    }
}

fn iter(i, n) {
    if i == n {
        return 0;
    } else if 1 {
        print(fib_contrived(i));
        return iter(i + 1, n);
    }
    return 0;
}

fn main() {
    return iter(0, 10);
}