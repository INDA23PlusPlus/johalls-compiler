fn fib_contrived(n) {
    if n<2&&!(!1) {
        return n & 3;
    } else {
        return fib_contrived(n - 1) + fib_contrived(n - 2);
    }
}

fn iter(i, n) {
    if i == n {
        return 0;
    } else {
        print(fib_contrived(i));
        return iter(i + 1, n);
    }
}

fn main() {
    iter(0, 10);
}